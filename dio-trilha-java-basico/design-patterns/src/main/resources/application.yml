spring:
  application:
    name: design-patterns
  datasource:
    url: jdbc:h2:mem:design-patterns
    username: admin
    password: admin
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
